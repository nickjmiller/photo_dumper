class AppConstants {
  static const String appTitle = 'Photo Dumper';
  static const String photoComparisonTitle = 'Photo Comparison';

  // Animation durations
  static const Duration photoAnimationDuration = Duration(milliseconds: 300);

  // Snackbar durations
  static const Duration snackbarDuration = Duration(seconds: 1);

  // Action messages

  // Instructions
  static const String swipeInstructions = 'Swipe right to keep, left to delete';
}
